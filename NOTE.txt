The registers before the mux are essentials because if I removed them and put one after the writeback mux, this will result in the values being read one clock cycle early, leading to an incorrect writeback.
